apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lakehouse-k3s-common
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/kkw855/lakehouse-k3s-gitops.git
    targetRevision: main
    path: apps/common/manifests
    directory:
      include: "*.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: lakehouse-k3s
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
